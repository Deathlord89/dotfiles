{
  # List contents of directory
  programs = {
    eza = {
      enable = true;
      enableFishIntegration = true;
      git = true;
      icons = "auto";
      extraOptions = [
        "--header"
      ];
    };
  };
}
